<?php

# Load the google url api config when the spark is loaded
$autoload['config'] = array('google_url_api');

# Load the google url api library when the spark is loaded
$autoload['libraries'] = array('google_url_api');
